# Architecture Decision Records

ADRs for {{PROJECT_NAME}} live in status subfolders:

- `proposed/` — under discussion
- `accepted/` — in force
- `deprecated/` — no longer preferred
- `superseded/` — replaced
- `rejected/` — declined

Use a short filename such as `0001-short-title.md`.
