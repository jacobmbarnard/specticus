# {{PROJECT_NAME}}

> Specifications as code.

This project was initialized with `scs init --style minimal`.

The **minimal** pack is a sparse vertical stub: document metadata, overview,
business requirements, diagrams, and ADRs. It exists to prove extra style packs
work — prefer `--style default` for a full Path A tree.

```bash
scs lint
scs build
scs open
```
